<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>McMinnville Pallet Co.</title>
    <!-- Favicon links -->
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="manifest" href="/site.webmanifest">
    <!-- Style links -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles.css">

</head>
<body>
    <header>
        <div class="header-top">
            <div class="header-contact">
                <span>📞 (931) 409-0868</span>
                <span>✉️ justincaten@hotmail.com</span>
                
                <% if (typeof user !== 'undefined' && user) { %>
                    <span style="margin-left: 15px; border-left: 1px solid rgba(255,255,255,0.3); padding-left: 15px;">
                        Logged in as: <strong><%= user.UserName || user.username %></strong>
                    </span>
                <% } %>
            </div>
        </div>
        <div class="header-main">
            <a href="/" class="logo">McMinnville Pallet Co.</a>
            <nav>
                <ul>
                    <li><a href="/" class="<%= (typeof page !== 'undefined' && page === 'home') ? 'active' : '' %>">Homepage</a></li>
                    <li><a href="/about" class="<%= (typeof page !== 'undefined' && page === 'about') ? 'active' : '' %>">About</a></li>
                    
                    <% if (typeof user !== 'undefined' && user) { %>
                        <li><a href="/orders" class="<%= (typeof page !== 'undefined' && page === 'orders') ? 'active' : '' %>">Orders (Admin)</a></li>
                        <li><a href="/logout" style="color: #ff6b35;">Logout</a></li>
                    <% } else { %>
                        <li><a href="/login" class="<%= (typeof page !== 'undefined' && page === 'login') ? 'active' : '' %>">Login</a></li>
                    <% } %>
                </ul>
            </nav>
        </div>
    </header>