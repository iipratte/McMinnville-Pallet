<!DOCTYPE html>
<html lang="en">

<!-- Header-->
<%- include('partials/header', { page: 'orders' }) %>

<body>

    <div class="main-content">
        
        <div class="page-header">
            <h2>Edit Order <span class="order-badge">#<%= order.id %></span></h2>
        </div>
        
        <div class="form-container">
            
            <form action="/editOrder/<%= order.id %>" method="POST">
                
                <div class="mb-4">
                    <label class="form-label">Customer Name</label>
                    <input type="text" name="customerName" class="form-control" value="<%= order.customer_name %>" required>
                </div>

                <div class="mb-4">
                    <label class="form-label">Product Name</label>
                    <input type="text" name="productName" class="form-control" value="<%= order.ProductName %>" required>
                </div>

                <div class="mb-4">
                    <label class="form-label">Quoted Price</label>
                    <div class="input-group">
                        <span class="input-group-text">$</span>
                        <input type="number" name="quotedPrice" class="form-control" value="<%= order.QuotedPrice %>" step="0.01" required>
                    </div>
                </div>

                <div class="mb-4">
                    <label class="form-label">Quantity</label>
                    <input type="number" name="quantity" class="form-control" value="<%= order.Quantity %>" required>
                </div>

                <div class="mb-4">
                    <label class="form-label">Order Status</label>
                    <select name="status" class="form-select">
                        <option value="Pending" <%= order.status == 'Pending' ? 'selected' : '' %>>Pending</option>
                        <option value="Completed" <%= order.status == 'Completed' ? 'selected' : '' %>>Completed (Shipped)</option>
                    </select>
                </div>

                <div class="form-actions">
                    <button type="submit" class="btn btn-primary">💾 Update Order</button>
                    <a href="/orders" class="btn btn-secondary">✖️ Cancel</a>
                </div>

            </form>
        </div>
    </div>

    <!-- Footer -->
     <%- include('partials/footer') %>
</body>
</html>